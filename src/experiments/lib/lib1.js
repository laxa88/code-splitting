export const add = (a, b) => console.log(`a + b: ${a + b}`);

export const sub = (a, b) => console.log(`a - b: ${a - b}`);

export const mul = (a, b) => console.log(`a * b: ${a * b}`);

export const div = (a, b) => console.log(`a / b: ${a / b}`);

export const prop = {
  a: 'prop a',
  b: 'prop b',
  c: 'prop c',
  d: 'prop d',
}

export default {
  add,
  sub,
  mul,
  div
}